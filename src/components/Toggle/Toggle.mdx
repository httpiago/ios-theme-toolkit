---
name: Toggle
route: /components/Toggle
menu: Components
---

import { Playground, PropsTable } from 'docz'
import { Toggle } from '../../index.js'
import '../../../dist/styles.css'

# Toggle

To use this component, simply import it into any file and pass the [modifiers](#/#props) in the properties.

```jsx
import { Toggle } from 'ios-theme-toolkit'

export default () => (
  <label>
    <Toggle checked={true} /> Label text
  </label>
)
```

## Examples

<Playground>
  <label>
    Green (default)
    <Toggle defaultChecked name="toggle-demo" value="wifi" />
  </label>

  <label>
    Blue
    <Toggle color="blue" name="toggle-demo" value="gps" defaultChecked={true} />
  </label>

  <label>
    Red (Disabled)
    <Toggle color="red" name="toggle-demo" value="bluetooth" defaultChecked={true} disabled />
  </label>

  <label>
    Yellow
    <Toggle color="yellow" name="toggle-demo" value="sync" defaultChecked={false} />
  </label>

  <label>
    Gray
    <Toggle color="gray" name="toggle-demo" value="battery" defaultChecked={true} />
  </label>

  <label>
    Small toggle
    <Toggle size="small" name="toggle-demo" value="small" defaultChecked />
  </label>

  <label>
    Big toggle
    <Toggle size="big" name="toggle-demo" value="small" defaultChecked />
  </label>

  <style>{".react-live-preview label { display: block; padding: 6px 0; } .react-live-preview .toggle__icon { margin-right: 6px !important; }"}</style>
</Playground>

## Props

<PropsTable of={ Toggle } />

All the properties which are not listed above will be transferred to the \<input type="checkbox"\> tag, so, It accepts all props which native checkbox support.
