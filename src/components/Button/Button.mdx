---
name: Button
route: /components/Button
menu: Componentes
fullpage: false
---

import { Playground, PropsTable } from 'docz'
import { Button } from '../../index.js'
import '../../../dist/styles.css'

# Button

To use this component, simply import it into any file and pass the [modifiers](#/#props) in the properties.

```jsx
import { Button } from 'ios-theme-toolkit'

export default () => (
  <Button fill color="green">Click me</Button>
)
```

## Examples

<Playground>
  <Button>Button</Button>
  <Button fill>Filled</Button>
  <Button outline>No borders</Button>
  <Button fill round>Rounded</Button>
  <Button size="big">BIG BUTTON</Button>
  <Button size="small">Small</Button>
  <Button fill size="small">Small filled</Button>
  <Button disabled>Disabled</Button>
  <Button href="#">Link button</Button>
  <span className="desc">Only icon:</span>
  <Button onlyIcon><svg width={18} height={18} viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth={2} strokeLinecap="round" strokeLinejoin="round"><path d="M22 17H2a3 3 0 0 0 3-3V9a7 7 0 0 1 14 0v5a3 3 0 0 0 3 3zm-8.27 4a2 2 0 0 1-3.46 0"/></svg></Button>
  <Button onlyIcon fill round color="gray"><svg width={18} height={18} viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth={2} strokeLinecap="round" strokeLinejoin="round"><path d="M22 17H2a3 3 0 0 0 3-3V9a7 7 0 0 1 14 0v5a3 3 0 0 0 3 3zm-8.27 4a2 2 0 0 1-3.46 0"/></svg></Button>
  <br />

  <Button block fill size="big" uppercase>Button that occupies the entire horizontal space</Button>

  <span className="desc">Colorful:</span>
  <Button fill color="red">Attention!</Button>
  <Button fill color="green">Success</Button>
  <Button fill color="yellow">Caution</Button>
  <Button fill color="gray">OK</Button>
  <span style={{ backgroundColor:'black', padding:6, display:'inline-block' }}>
    <Button fill color="white">White</Button>
  </span>
  <Button fill color="black">Black</Button>
  <br />
  <Button color="red">Red</Button>
  <Button color="green">Green</Button>
  <Button color="yellow">Yellow</Button>
  <Button color="gray">Gray</Button>
  <span style={{ backgroundColor:'black', padding:6, display:'inline-block' }}>
    <Button color="white">White</Button>
  </span>
  <Button fill color="black">Black</Button>

  <style>{".react-live-preview .desc { display: block; font-weight: bold; margin-top: 20px; }"}</style>
  <style>{" .react-live-preview .button { margin: 0 3px; } "}</style>
</Playground>

## Props

<PropsTable of={Button} />

All the properties which are not listed above will be transferred to the \<button\> tag, so, It accepts all props which native button support.
